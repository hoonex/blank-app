import { chromium } from 'playwright';
import { mkdir, writeFile } from 'node:fs/promises';

const BASE=process.env.FLOW_BASE_URL||'http://127.0.0.1:4173';
const OUT=process.env.FLOW_TEST_OUT||'school-device-matrix-audit';
const CASES=[
  {name:'phone-360',viewport:{width:360,height:800},isMobile:true,hasTouch:true},
  {name:'phone-390',viewport:{width:390,height:844},isMobile:true,hasTouch:true},
  {name:'phone-412',viewport:{width:412,height:915},isMobile:true,hasTouch:true},
  {name:'phone-landscape',viewport:{width:844,height:390},isMobile:true,hasTouch:true},
  {name:'tablet-portrait',viewport:{width:768,height:1024},isMobile:false,hasTouch:true},
  {name:'tablet-landscape',viewport:{width:1024,height:768},isMobile:false,hasTouch:true},
  {name:'desktop-1280',viewport:{width:1280,height:800},isMobile:false,hasTouch:false},
  {name:'desktop-1366',viewport:{width:1366,height:768},isMobile:false,hasTouch:false},
  {name:'desktop-1920',viewport:{width:1920,height:1080},isMobile:false,hasTouch:false},
];
const SCHOOL={officeCode:'D10',officeName:'대구광역시교육청',schoolCode:'7240101',name:'정동고등학교',englishName:'Jeongdong High School',kind:'고등학교',location:'대구광역시',jurisdiction:'대구광역시동부교육지원청',type:'사립',postalCode:'41063',address:'대구광역시 동구 반야월북로 199',addressDetail:'',phone:'053-000-0000',fax:'053-000-0001',homepage:'https://jungdong.dge.hs.kr',coed:'남녀공학',highSchoolType:'일반고',highSchoolTrack:'일반계',dayNight:'주간',founded:'19830301',anniversary:'19830301'};
const logoSvg='data:image/svg+xml;base64,'+Buffer.from('<svg xmlns="http://www.w3.org/2000/svg" width="96" height="96"><rect width="96" height="96" rx="22" fill="#eee"/><path d="M20 48h56M48 20v56" stroke="#555" stroke-width="8"/></svg>').toString('base64');
const pad=n=>String(n).padStart(2,'0');
const ymd=d=>`${d.getFullYear()}${pad(d.getMonth()+1)}${pad(d.getDate())}`;
const parseYmd=v=>new Date(+v.slice(0,4),+v.slice(4,6)-1,+v.slice(6,8),12);
const plusDay=(v,n)=>{const d=parseYmd(v);d.setDate(d.getDate()+n);return ymd(d)};
function rows(date){return['문학','영어Ⅱ','선택과목','선택과목','스포츠 생활2','선택과목','진로활동'].map((subject,i)=>({date,period:i+1,subject,grade:'2',className:'6'}))}
function dashboard(selected){
  const d=parseYmd(selected),day=d.getDay(),monday=new Date(d);monday.setDate(d.getDate()+(day===0?-6:1-day));
  const map=new Map();for(let i=0;i<5;i++){const date=ymd(new Date(monday.getFullYear(),monday.getMonth(),monday.getDate()+i,12));for(const row of rows(date))map.set(`${date}:${row.period}`,row)}for(const row of rows(selected))map.set(`${selected}:${row.period}`,row);
  return{school:SCHOOL,selected,from:ymd(monday),to:ymd(new Date(monday.getFullYear(),monday.getMonth(),monday.getDate()+4,12)),timetable:[...map.values()],meals:[{date:selected,type:'중식',dishes:['현미밥','닭갈비(5.6.15.)','계란찜(1.)','배추김치(9.)'],calories:'742 Kcal',nutrition:'탄수화물 90g\n단백질 32g',origin:'쌀 국내산\n닭고기 국내산',people:'320'}],events:[{date:plusDay(selected,2),name:'2학기 학급 행사',content:'학급별 행사',grade1:'N',grade2:'Y',grade3:'N',holidayType:''},{date:plusDay(selected,7),name:'진로 체험',content:'진로 체험 활동',grade1:'Y',grade2:'Y',grade3:'Y',holidayType:''}],scheduleMeta:{mode:'fixture',count:2}};
}
function json(route,body,status=200,headers={}){return route.fulfill({status,contentType:'application/json; charset=utf-8',headers,body:JSON.stringify(body)})}
async function fixtures(page){
  await page.route('**/functions/v1/school-data**',async route=>{const u=new URL(route.request().url()),action=u.searchParams.get('action')||'search';if(action==='search')return json(route,{schools:[SCHOOL]});if(action==='classes')return json(route,{classes:['1','2','3','4','5','6','7','8']});if(action==='media')return json(route,{media:{hero:'',logo:logoSvg,logoSource:'fixture-official'},homepage:SCHOOL.homepage},200,{'cache-control':'public, max-age=86400'});if(action==='place')return json(route,{provider:'kakao',place:{id:'school-place',name:SCHOOL.name,url:'https://place.map.kakao.com/7240101',address:SCHOOL.address,roadAddress:SCHOOL.address,phone:SCHOOL.phone,x:'128.687',y:'35.875',distance:'0'}});if(action==='dashboard')return json(route,dashboard(u.searchParams.get('date')||ymd(new Date())));return json(route,{error:`unknown fixture ${action}`},404)});
  await page.route('**/functions/v1/school-logo**',route=>route.fulfill({status:204,body:''}));
}
async function stubs(context){await context.addInitScript(()=>{Object.defineProperty(navigator,'share',{configurable:true,value:async data=>{window.__flowShare=data}});try{Object.defineProperty(navigator,'geolocation',{configurable:true,value:{getCurrentPosition:ok=>setTimeout(()=>ok({coords:{latitude:35.875,longitude:128.687,accuracy:8}}),0)}})}catch{}})}
function watch(page){const consoleErrors=[],pageErrors=[],failed=[];page.on('console',m=>{if(m.type()==='error')consoleErrors.push(m.text())});page.on('pageerror',e=>pageErrors.push(String(e)));page.on('requestfailed',r=>{if(!/fonts\.gstatic|fonts\.googleapis/.test(r.url()))failed.push({url:r.url(),error:r.failure()?.errorText||''})});return{consoleErrors,pageErrors,failed}}
function assertNoErrors(name,errors){if(errors.consoleErrors.length||errors.pageErrors.length||errors.failed.length)throw new Error(`${name}: browser errors ${JSON.stringify(errors)}`)}
async function visibleClick(page,selector){const all=page.locator(selector),count=await all.count(),vp=page.viewportSize();for(let i=0;i<count;i++){const item=all.nth(i);if(!await item.isVisible())continue;await item.scrollIntoViewIfNeeded().catch(()=>{});const b=await item.boundingBox();if(!b||!vp||b.right<=0||b.bottom<=0||b.x>=vp.width||b.y>=vp.height)continue;await item.click();return item}throw new Error(`No visible target for ${selector}`)}
async function shiftDate(page,delta){const dock=page.locator('#flowTodayDateDock');if(await dock.isVisible()){const before=await page.locator('#datePicker').inputValue(),b=await dock.boundingBox();if(!b)throw new Error('date dock has no bounds');const x=b.x+b.width/2,y=b.y+b.height/2;await page.mouse.move(x,y);await page.mouse.down();await page.mouse.move(x+(delta>0?-46:46),y,{steps:4});await page.mouse.up();await page.waitForFunction(v=>document.querySelector('#datePicker')?.value!==v,before,{timeout:1600});return}await visibleClick(page,delta>0?'#nextDay':'#prevDay')}
async function shell(page,c,label){
  const state=await page.evaluate(()=>{const visible=e=>{if(!e)return false;const s=getComputedStyle(e),r=e.getBoundingClientRect();return s.display!=='none'&&s.visibility!=='hidden'&&Number(s.opacity)!==0&&r.width>0&&r.height>0};const rect=e=>e?(()=>{const r=e.getBoundingClientRect();return{left:r.left,right:r.right,top:r.top,bottom:r.bottom,width:r.width,height:r.height}})():null;const nav=document.querySelector('#bottomNav'),top=document.querySelector('.mobile-topbar'),side=document.querySelector('.desktop-sidebar'),before=nav?getComputedStyle(nav,'::before'):null,copy=nav?.querySelector('.flow-refraction-copy-lens');return{ui:document.documentElement.dataset.flowSchoolUi||'',global:document.documentElement.dataset.flowSchoolGlobalShell||'',glass:document.documentElement.dataset.flowGlassMode||'',refraction:document.documentElement.dataset.flowGlassRefraction||'',copyFlag:document.documentElement.dataset.flowRefractionCopy||'',navVisible:visible(nav),topVisible:visible(top),sideVisible:visible(side),navRect:rect(nav),topRect:rect(top),navRadius:nav?getComputedStyle(nav).borderRadius:'',lensRadius:before?.borderRadius||'',lensTransform:before?.transform||'',lensBackground:before?.backgroundImage||'',lensShadow:before?.boxShadow||'',copyVisible:visible(copy),copyTransform:copy?getComputedStyle(copy).transform:''}});
  if(state.ui!=='v2'||state.global!=='v1')throw new Error(`${c.name} ${label}: final School shell missing ${JSON.stringify(state)}`);
  const desktop=c.viewport.width>=1181;
  if(desktop){if(!state.sideVisible||state.topVisible||state.navVisible)throw new Error(`${c.name} ${label}: desktop shell leaked phone chrome ${JSON.stringify(state)}`)}
  else{
    if(state.sideVisible||!state.topVisible||!state.navVisible)throw new Error(`${c.name} ${label}: touch shell geometry wrong ${JSON.stringify(state)}`);
    if(!state.topRect||Math.abs(state.topRect.left)>1.5||Math.abs(state.topRect.right-c.viewport.width)>1.5)throw new Error(`${c.name} ${label}: top shell not edge-to-edge ${JSON.stringify(state.topRect)}`);
    if(!state.navRect||parseFloat(state.navRadius)<state.navRect.height/2)throw new Error(`${c.name} ${label}: bottom nav is not maximum pill curvature ${JSON.stringify(state)}`);
    if(parseFloat(state.lensRadius)<Math.min(20,state.navRect.height/2-5))throw new Error(`${c.name} ${label}: follower lens lost pill curvature ${JSON.stringify(state)}`);
    if(state.glass!=='optical'&&(state.lensBackground==='none'||state.lensShadow==='none'))throw new Error(`${c.name} ${label}: standard liquid follower is visually missing ${JSON.stringify(state)}`);
  }
  return state;
}
async function shot(page,name){await page.screenshot({path:`${OUT}/${name}.png`,fullPage:false,animations:'disabled'})}

await mkdir(OUT,{recursive:true});
const browser=await chromium.launch({headless:true});
const report={generatedAt:new Date().toISOString(),cases:[],failures:[]};
for(const c of CASES){
  const context=await browser.newContext({viewport:c.viewport,isMobile:c.isMobile,hasTouch:c.hasTouch,deviceScaleFactor:1,locale:'ko-KR',timezoneId:'Asia/Seoul',colorScheme:'light'});await stubs(context);const page=await context.newPage();page.setDefaultTimeout(12000);await fixtures(page);const errors=watch(page);const result={name:c.name,viewport:c.viewport,shells:{}};
  try{
    await page.goto(BASE,{waitUntil:'domcontentloaded'});await page.locator('.school-search-panel').waitFor();await page.locator('#schoolSearch').fill('정동고');await page.locator('#schoolSearchBtn').click();await page.locator('#schoolResults [data-result-index]').first().click();await page.locator('#setupDialog').waitFor({state:'visible'});await page.locator('#gradeRow [data-grade="2"]').click();await page.locator('#classRow [data-class="6"]').waitFor();await page.locator('#classRow [data-class="6"]').click();await page.locator('#setupSave').click();await page.locator('#dashboard:not(.hidden)').waitFor();await page.locator('#timetable [data-period]').first().waitFor();await page.waitForFunction(()=>document.documentElement.dataset.flowSchoolGlobalShell==='v1');
    result.shells.today=await shell(page,c,'today');await shot(page,`${c.name}-today`);
    const standardLens=result.shells.today.lensTransform;
    await shiftDate(page,1);await shiftDate(page,-1);
    await page.locator('#editSubjectsBtn').click();const first=page.locator('#timetable [data-period]').first();await first.click();await page.locator('#subjectDialog').waitFor({state:'visible'});await page.locator('#customSubjectInput').fill('인공지능 기초');await page.locator('#saveSubjectBtn').click();if(!(await page.evaluate(()=>localStorage.getItem('flow-school-overrides-v2')||'')).includes('인공지능 기초'))throw new Error(`${c.name}: subject edit did not persist`);await first.click();await page.locator('#resetSubjectBtn').click();
    await page.locator('#allergyBtn').click();await page.locator('#allergyDialog').waitFor({state:'visible'});const allergy=page.locator('#allergyGrid input,#allergyGrid button').first();if(await allergy.count())await allergy.click();await page.locator('#saveAllergyBtn').click();if(await page.locator('#mealDetailBtn').isVisible()){await page.locator('#mealDetailBtn').click();if(await page.locator('#mealDetails').evaluate(e=>e.classList.contains('hidden')))throw new Error(`${c.name}: meal detail failed`)}
    await page.locator('#shareTimetableBtn').click();if(!(await page.evaluate(()=>window.__flowShare?.text||'')).includes(SCHOOL.name))throw new Error(`${c.name}: share failed`);
    await visibleClick(page,'#settingsBtn,#mobileSettingsBtn');await page.locator('#flowSchoolSettingsView:not(.hidden)').waitFor();result.shells.settings=await shell(page,c,'settings');await shot(page,`${c.name}-settings`);
    await page.locator('#flowSchoolSettingsView [data-flow-settings-theme="dark"]').click();await page.waitForFunction(()=>document.documentElement.dataset.themeMode==='dark');await page.locator('#flowSchoolSettingsView [data-flow-settings-theme="light"]').click();await page.waitForFunction(()=>document.documentElement.dataset.themeMode==='light');
    await page.locator('#flowSchoolSettingsView [data-flow-bell="start"]').fill('08:20');await page.locator('#flowSchoolSettingsView [data-flow-bell="lesson"]').fill('50');await page.locator('#flowSchoolSettingsView [data-flow-bell="break"]').fill('10');await page.locator('#flowSchoolSettingsView [data-flow-bell="meal"]').fill('12:10');
    await page.locator('#flowSchoolSettingsView [data-flow-settings-glass="optical"]').click();await page.waitForFunction(()=>document.documentElement.dataset.flowGlassMode==='optical');
    if(c.viewport.width<1181){await page.waitForFunction(()=>['true','fallback'].includes(document.documentElement.dataset.flowGlassRefraction||''));const optical=await shell(page,c,'settings optical');if(optical.refraction==='true'&&!optical.copyVisible)throw new Error(`${c.name}: optical refraction copy lens missing ${JSON.stringify(optical)}`);result.shells.optical=optical}
    await page.locator('#flowSchoolSettingsView [data-flow-save-school]').click();const bell=await page.evaluate(()=>JSON.parse(localStorage.getItem('flow-school-bell-v1')||'{}'));if(bell.start!=='08:20'||bell.meal!=='12:10')throw new Error(`${c.name}: settings save failed ${JSON.stringify(bell)}`);
    await visibleClick(page,'[data-view="today"]');await page.locator('#todayView:not(.hidden)').waitFor();const weekToggle=page.locator('.timetable-mode-toggle [data-timetable-mode="week"],.timetable-mode-toggle [data-view="week"]').first();await weekToggle.scrollIntoViewIfNeeded();await weekToggle.click();await page.locator('#inlineWeekTimetable:not(.hidden)').waitFor();if(!await page.locator('#editSubjectsBtn').isVisible()||!await page.locator('#shareTimetableBtn').isVisible())throw new Error(`${c.name}: Week lost edit/share`);result.shells.week=await shell(page,c,'week');await page.locator('#nextWeek').click();await page.waitForTimeout(80);await page.locator('#thisWeekBtn').click();await shot(page,`${c.name}-week`);
    await visibleClick(page,'[data-view="schedule"]');await page.waitForFunction(()=>!document.querySelector('#scheduleView')?.classList.contains('hidden'));result.shells.schedule=await shell(page,c,'schedule');await page.locator('#nextMonth').click();await page.waitForTimeout(60);await page.locator('#prevMonth').click();if(!(await page.locator('#calendarGrid .calendar-day').count()))throw new Error(`${c.name}: calendar missing`);await shot(page,`${c.name}-schedule`);
    await visibleClick(page,'[data-view="school"]');await page.waitForFunction(()=>!document.querySelector('#schoolView')?.classList.contains('hidden'));await page.locator('#schoolInfoGrid .info-tile').first().waitFor();await page.waitForFunction(()=>document.querySelector('#mapLink')?.dataset.mapResolved==='true');result.shells.school=await shell(page,c,'school');await page.locator('#rankTotal').fill('240');await page.locator('#rankValue').fill('23');if((await page.locator('#rankMain').textContent())?.includes('값을 입력'))throw new Error(`${c.name}: rank calculator failed`);await shot(page,`${c.name}-school`);
    if(c.viewport.width<1181&&result.shells.schedule.lensTransform===standardLens&&result.shells.schedule.copyTransform===result.shells.settings.copyTransform)throw new Error(`${c.name}: follower lens did not move between tabs`);
    await visibleClick(page,'#schoolBtn,#mobileSchoolBtn');await page.locator('#switchDialog').waitFor({state:'visible'});await page.locator('#switchDialog .dialog-close').click();await visibleClick(page,'#schoolBtn,#mobileSchoolBtn');await page.locator('#changeSchoolBtn').click();await page.locator('#landing:not(.hidden)').waitFor();assertNoErrors(c.name,errors);result.pass=true;
  }catch(error){result.pass=false;result.error=error?.stack||String(error);report.failures.push({case:c.name,error:result.error});console.error(`${c.name}: FAIL\n${result.error}`)}finally{report.cases.push(result);await context.close()}
}
await writeFile(`${OUT}/school-device-matrix-report.json`,JSON.stringify(report,null,2));await browser.close();if(report.failures.length)throw new Error(`School device matrix found ${report.failures.length} failure(s): ${JSON.stringify(report.failures.map(x=>({case:x.case,error:x.error.split('\n')[0]})))}`);console.log(`School device matrix PASS: ${CASES.length} viewports`);
