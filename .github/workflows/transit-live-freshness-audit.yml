name: Transit live freshness audit

on:
  pull_request:
    paths:
      - 'supabase/functions/transit-data-core/index.ts'
      - 'tests/transit-live-freshness-audit.mjs'
      - '.github/workflows/transit-live-freshness-audit.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  transit-live-freshness:
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Verify Transit arrival freshness and reranking contract
        run: node tests/transit-live-freshness-audit.mjs
      - name: Verify Edge secret/access inventory remains clean
        run: node scripts/edge-secret-audit.mjs
