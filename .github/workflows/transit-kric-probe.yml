name: Transit KRIC compatibility probe retired
on:
  workflow_dispatch:
jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'KRIC compatibility probe retired; separate KRIC service approval is required.'
